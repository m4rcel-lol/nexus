{
  "name": "nexus",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @nexus/shared && npm run build --workspace @nexus/api && npm run build --workspace @nexus/web",
    "dev": "npm run dev --workspace @nexus/api & npm run dev --workspace @nexus/web",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run build --workspace @nexus/shared && npm run test --workspaces --if-present",
    "prisma:generate": "prisma generate --schema packages/db/prisma/schema.prisma",
    "prisma:migrate": "prisma migrate deploy --schema packages/db/prisma/schema.prisma"
  },
  "devDependencies": {
    "prisma": "6.19.3",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  }
}
